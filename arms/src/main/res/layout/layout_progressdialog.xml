<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/transparent">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:alpha="0.70"
        android:background="@drawable/bg_load_progress"
        android:gravity="center"
        android:minHeight="65dp"
        android:minWidth="65dp"
        android:orientation="vertical"
        android:padding="@dimen/space15">

        <!--<ProgressBar
            android:id="@+id/loadingPB"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:indeterminateDrawable="@anim/loading_progress"
            android:indeterminate="false"/>-->

        <me.jessyan.mvparms.demo.widget.CircularProgressBar
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:layout_marginTop="@dimen/space2"
            android:indeterminate="true"
            android:indeterminateOnly="false"
            app:cpb_color="#fef8f8f8"
            app:cpb_stroke_width="2dp" />

        <TextView
            android:id="@+id/loadingTV"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="@dimen/space3"
            android:layout_marginRight="@dimen/space2"
            android:layout_marginTop="@dimen/space8"
            android:gravity="center"
            android:text="加载中..."
            android:textColor="#fef8f8f8"
            android:visibility="visible" />

    </LinearLayout>

</FrameLayout>